name: e_tacho
description: "Driver working-time assistant (EU 561/2006 + PL) — MVP."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  timezone: ^0.11.0
  compliance_engine:
    path: packages/compliance_engine
  drift: ^2.34.0
  sqlite3_flutter_libs: ^0.6.0+eol
  path_provider: ^2.1.6
  path: ^1.9.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.1+1
  build_runner: ^2.15.0

flutter:
  generate: true
  uses-material-design: true
