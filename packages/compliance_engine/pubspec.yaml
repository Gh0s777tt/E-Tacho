name: compliance_engine
description: >-
  Pure-Dart rules engine for driver working-time compliance
  (EU Regulation 561/2006 + Polish Drivers' Working Time Act).
  No Flutter, UI or database dependencies.
version: 0.1.0
publish_to: 'none'
repository: https://github.com/Gh0s777tt/E-Tacho

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  collection: ^1.19.1
  equatable: ^2.0.8
  meta: ^1.18.3
  timezone: ^0.11.0
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
